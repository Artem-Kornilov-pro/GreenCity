# Инструменты разработки. Отдельно от requirements.txt намеренно: тот ставится
# внутрь backend-образа (backend/Dockerfile), и линтеру/тестам там делать
# нечего. Ставится в локальный venv через `make venv`.
-r requirements.txt

ruff==0.16.6

# --- Тесты (backend/tests/, parser/tests/) ----------------------------------
# httpx -- не сам тестовый фреймворк, а зависимость fastapi.testclient.TestClient
# (Starlette использует httpx как HTTP-клиент внутри процесса, без реального
# сокета). mongomock/mongomock-motor/fakeredis подменяют MongoDB/Redis in-memory
# реализациями с тем же асинхронным интерфейсом (AsyncMongoClient/redis.asyncio)
# -- тесты не требуют поднятых mongod/redis-server и не трогают реальную
# инфраструктуру.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.28.1
mongomock==4.3.0
mongomock-motor==0.0.36
fakeredis==2.38.0
